<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @post.title %>
</p>

<p>
  <b>Description:</b>
  <%= @post.title %>
</p>

<p>
  <b>Comments:</b>
  <% @comments.each do |comment| %>
    <%#=	 comment.inspect %>
      <p><%= comment.comment %><%= link_to 'Delete',delete_comment_post_path(comment) ,:method => "DELETE", :data => { :confirm => 'Are you sure?' } rescue nil %></p>
 <%end %>
</p>

<%= form_for(@comment,:url=> create_comment_post_path) do |f| %>
    <div class="field">
      <%= f.label :comment %><br />
      <%= f.text_area :comment %>  <br/>
    </div> <br />

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

<%=	 link_to "Back" ,mydatalist_posts_path %>


